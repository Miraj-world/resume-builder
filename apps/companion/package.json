{
  "name": "@resume-builder/companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "analyze": "tsx src/cli.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --exclude dist/**"
  },
  "dependencies": {
    "@resume-builder/contracts": "*"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "tsx": "4.22.4",
    "vitest": "4.1.9"
  }
}
